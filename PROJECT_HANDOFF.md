# BlogVkC 项目交接

更新日期：2026-09-14。

## 来源与当前基线

- 承接任务：`OUTDATED_创建博客网站`，任务 ID `019eab77-f53a-7633-960f-88abacb3195c`。
- 当前接续任务：`完善博客网站项目`，任务 ID `01a0a00b-76db-7382-bb60-ae8caf10ee79`。
- 工作目录：`D:\Workspace\UserLibrary\BlogVkC`。
- 当前分支 `main`，HEAD `6085cb8`（text changed）。本地记录的 `origin/main` 与 HEAD 相同；本次没有 fetch，不能据此断言实时远端状态。
- 本记录根据旧任务最近 20 轮记录、项目源码和 Git 历史整理；不是全部历史的逐字备份。更早的具体设计问题可继续按任务 ID 分页读取。
- 旧任务最后三个回合因上下文窗口耗尽失败，读取结果没有提供这些回合的用户消息，不能推测其中有新的功能要求。
- 旧任务最后完成的工作是手机立绘贴合修正。虽然当时回复写着尚未 push，但这组修改已经进入后续提交 `ce0cbdb`；不要按旧回复重新应用或回滚。
- 此后还有用户的素材、文案、favicon 更新，以当前工作区为基线。

## 已恢复的用户要求

1. 网站名为 `VkC's Blog`，展示个人原创角色「维嘉」与个人贴文，部署到 GitHub Pages。
2. 使用 Fluent Design 2，主色 `#c6bae0`，支持亮／暗主题和繁体／简体切换。
3. 手机端需要独立首页布局，按 iPhone、Android 和小屏特点组织内容。不要把桌面首页直接缩小后当作手机版。
4. 最新手机首页顺序是标题与欢迎语 → 立绘 → 三个入口卡片。立绘底部必须与第一张入口卡片贴合，不能浮空。此前把立绘变成背景、被卡片遮住或纸片散落的布局已经被否定。
5. 卡片使用稳定表面、轻阴影、边框和 semibold 标题，亮暗主题遵循一致的 token 映射。
6. UI 图标使用 Fluent System Icons。入口图标本体要清晰、足够大，不要再外套多余灰色矩形。交互时出现基于角色紫色的渐变边框。
7. 用户尚未上传正文；阅读按钮暂时从底部居中弹出 `Work in progress...`，带进入／退出动画与品牌色提示。这是明确要求，不能把它误判成需要自行编写正文的缺陷。
8. 用户明确说明红底／遮挡素材用于避免读取原立绘；不要擅自去除遮挡、恢复图像或替换原始素材。
9. 图片区域保留右键菜单及拖拽拦截，范围包括首页、图件与札记；普通文字区域保持正常操作。
10. 桌面装饰中，`4_guide.png` 要贴左边缘，右上蕾丝贴 header 下缘及窗口右边，不要缩进内容矩形。手机端的大纸层和四张小纸片目前隐藏。

以上是用户设计约定的摘要，不代表本轮已重新审查整个网站是否符合 Fluent 规范。

## 项目结构与现有功能

- React 19 + TypeScript + Vite 8，Fluent UI React v9 与 `@fluentui/react-icons`。
- `src/App.tsx`：主要页面、makeStyles 样式、主题、双语文案、导航、图册切换、分享和 WIP 提示。
- `src/App.css`：全局样式、字体、滚动和主题切换期间的动画处理。
- 四个页面锚点：`hero`、`charts`、`blog`、`regulation`。
- `contentByLocale` 保存 `zh-TW`／`zh-CN` 内容；主题和语言选择使用 localStorage。
- `getIsMobileDevice` 根据 UA、窄视口和粗指针判断移动端；首页在 JSX 中有独立移动端分支。
- 图件包含拼贴、肖像与场景，札记现有三个封面／摘要卡片。
- `Image/` 保存角色和首页装饰素材。首页立绘目前 import `hero_figure_placeholder.png` 并赋给 `heroFigure`；文件名不代表允许替换素材。
- `public/favicon.svg` 是网页图标入口；`index.html` 设置初始标题和 viewport。
- `.github/workflows/pages.yml` 在 main push 时安装依赖、构建 dist 并部署 GitHub Pages，使用 Node 22。
- `vite.config.ts` 使用相对 base `./`。

## 本轮验证

- `npm.cmd run build` 成功，TypeScript 和 Vite 构建均通过。
- 构建有 JS chunk 超过 500 kB 的提示：当前主 chunk 约 511.44 kB，gzip 143.47 kB。
- 多张图片达数 MB，后续可评估加载性能；本轮未修改图片。
- 本轮没有重新做浏览器截图、触摸交互或真机测试，也没有检查在线部署状态。
- 旧任务曾报告 360／412／430 px 无横向溢出，立绘与卡片贴合；这是旧版本测试结果，不能代替当前素材版本的视觉回归。
- 接手时已跟踪源码没有未提交修改；`.agents/` 与 `skills-lock.json` 是已有的未跟踪技能安装文件。
- 本轮仅新增此交接记录，没有改动网站实现、提交或推送。

## 接下来接续的工作

1. 按用户新的具体需求继续完善；当前接续请求没有指定新增模块，不自行重做整站。
2. 下一轮涉及首页布局时，先检查当前素材下手机立绘的可见底部与首张卡片是否真正贴合。仅测图片元素边界不足以证明素材内容贴合。
3. 页面改动后按影响范围检查桌面亮暗主题、360／412／430 px 手机宽度，必要时补充横屏、平板和真实 Safari 检查。
4. 涉及交互时检查语言／主题持久化、锚点导航、图册切换、WIP 提示和键盘操作。
5. 正文继续保留 WIP，直到用户提供内容或明确提出文章功能需求。
6. 性能优化与结构拆分仅作为后续候选，不是已经确认的缺陷或本轮承诺。

## 常用命令

```powershell
npm.cmd run dev
npm.cmd run build
npm.cmd run preview -- --port 4173
git status --short
```

设计参考沿用旧任务提供的 Fluent 2 官网、Fluent UI React Storybook 与 microsoft/fluentui；本轮为交接核对，未重新浏览这些文档。
